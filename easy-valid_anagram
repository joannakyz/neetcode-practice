class Solution:
    def isAnagram(self, s, t):
        if sorted(s) == sorted(t):
            return True
        else:
            return False
            
a = 'racecar'
b = "carrace"

sol = Solution()
result = print(sol.isAnagram(a, b))
